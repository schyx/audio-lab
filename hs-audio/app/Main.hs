module Main (main) where

import Lib

main :: IO ()
main = do
  -- sound <- getSound "./sounds/crash.wav" False
  -- let reversed = backwards sound
  -- writeSound "backwards_crash.wav" reversed
  return ()
